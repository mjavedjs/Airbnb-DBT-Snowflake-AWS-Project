version: 2

models:
  - name: bronze_Boooking
    config:
      materialized: table
  - name: bronze_host
    config:
      materialized: table
  - name: bronze_listinings
    config:
      materialized: table